# README for Container apps

This file describes the `/app` folder that is copied into the iVerilog container.

## Files
### html_decode.py
A trivial Python 3 script that accepts a file through `stdin`, html escapes it, then writes it to a file.

### Verilog files
Any Verilog files in this directory are to test the iVerilog container without having to rely on generated content.