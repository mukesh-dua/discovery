SECTION 1 - SPECIFIC INSTRUCTIONS AND RULES FOR ROUTER YAML (Static):

🚨 **CRITICAL EVENT NAMING — USE EXACT PRE-COMPUTED NAMES**:
The event names for router actions have been pre-computed. You MUST use these exact names — do NOT invent, abbreviate, or modify them in any way:

{event_name_mapping}

These names are case-sensitive. Use them exactly as shown in:
- Router `extension.events[].name` fields
- Router instructions (the list of events the router can emit)
- Include ONLY these events (no extras, no omissions)

You must create a routing agent YAML that:
- Routes requests ONLY to the selected specialized agents provided below
- Uses proper JSON output format: {"NextAgent": "<name>", "Response": "<reasoning>"}
- Includes events ONLY for selected agents (no hardcoded events for unselected agents)
- Follows the EXACT YAML structure from the reference template including ALL sections
- MUST include complete model parameters: top_p, temperature, response_format
- MUST include complete extension block: events, inputs, outputs, system_prompts
- Uses double braces for template variables: {{nodePoolContext}}, {{dataHandlingContext}}
- Must NOT reference any agents not in the selected list

**CRITICAL YAML STRUCTURE REQUIREMENTS:**
- Include ALL model parameters from template (top_p: 0, temperature: 0, response_format: auto)
- Include COMPLETE extension block with all arrays and objects (events: [], inputs: [], outputs: [], system_prompts: {})
- NEVER use template variables as YAML keys (e.g., NOT "{{agentTeam}}: value")
- NEVER add extra quotes around template variables (e.g., NOT "{{agentTeam}}" or ""{{agentTeam}}"")
- Preserve ALL template variables exactly as shown: {{nodePoolContext}}, {{dataHandlingContext}}
- Do NOT modify the base instructions template variables - only update agent-specific routing logic

SECTION 2 - DYNAMIC CONTEXT (User-Specific):

**Entry Agent System Details:**
- System Name: {component_agent_name}
- User-Provided Description: {description}
- Primary Domain: {description_lower}
- Component Role: Router Agent

**IMPLICIT Entry Agent Components (Always Available):**
- **{unique_planner_name}**: Planning component that breaks down user requests into actionable steps
- **{unique_summarizer_name}**: Summarization component that consolidates results into final responses

**SELECTED Specialized Agents to Route To (ONLY THESE):**
{selected_agents_list}

**Selected Specialized Agent Capabilities:**
{selected_agents_capabilities}

SECTION 3 - REPRESENTATIVE SAMPLE (Reference Template):

```yaml
{sample_content}
```

**CRITICAL TRANSFORMATION REQUIREMENTS:**
1. MAINTAIN the EXACT structure from the sample above - every section is mandatory
2. REPLACE the sample agent name with the correct component name: **{component_agent_name}**
3. REPLACE the hardcoded events in the sample with events ONLY for selected agents
4. REPLACE hardcoded routing options with options ONLY for selected agents  
5. REPLACE hardcoded agent descriptions with descriptions ONLY for selected agents
6. UPDATE the instructions section to reference ONLY the selected agents
7. REMOVE any mention of agents not in the selected list
8. PRESERVE ALL model parameters exactly as shown (top_p: 0, temperature: 0, response_format: auto)
9. PRESERVE the complete extension block exactly as shown (events: [], inputs: [], outputs: [], system_prompts: {})
10. KEEP template variables {{nodePoolContext}} and {{dataHandlingContext}} unchanged
11. MAINTAIN the exact YAML structure and template variable formatting

**Output Format:**
Return ONLY the final YAML document (no extra commentary or code fences). The YAML must be a COMPLETE agent definition with ALL sections from the sample template, customized to route ONLY to the selected agents.
